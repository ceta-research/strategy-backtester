# Phase 8A measurement config — tiny scope for local parquet A/B.

static:
  strategy_type: momentum_top_gainers
  start_margin: 10000000
  start_epoch: 1577836800   # 2020-01-01
  end_epoch: 1640908800     # 2021-12-31
  prefetch_days: 250
  data_granularity: day
  data_provider: parquet

scanner:
  instruments:
    - [{exchange: NSE, symbols: []}]
  price_threshold: [50]
  avg_day_transaction_threshold:
    - {period: 30, threshold: 10000000}
  n_day_gain_threshold:
    - {n: 5, threshold: -999}

entry:
  momentum_lookback_days: [63]
  top_n_pct: [0.30]
  rebalance_interval_days: [21]
  min_momentum_pct: [0]
  regime_instrument: ['']
  regime_sma_period: [0]
  direction_score_n_day_ma: [0]
  direction_score_threshold: [0]
  universe_mode: ["full_period"]   # overridden by harness

exit:
  trailing_stop_pct: [15]
  max_hold_days: [63]

simulation:
  default_sorting_type: [top_gainer]
  order_sorting_type: [top_gainer]
  order_ranking_window_days: [30]
  max_positions: [10]
  max_positions_per_instrument: [1]
  order_value_multiplier: [1.0]
  max_order_value:
    - {type: fixed, value: 1000000000}
