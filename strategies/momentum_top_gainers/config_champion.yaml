# Champion configs after 2100+ config sweep across full parameter ranges
#
# Three profiles found (all share: mom=189d, rebal=42d, hold=378d, regime=NIFTYBEES>SMA200):
#
# AGGRESSIVE: TSL=35% pos=12 dir>0.45 -> CAGR=20.2% MDD=-24.6% Cal=0.82 (134 trades)
# BALANCED:   TSL=22% pos=12 dir>0.45 -> CAGR=17.8% MDD=-22.6% Cal=0.79 (186 trades)
# CONSERVATIVE: TSL=22% pos=30 dir>0.50 -> CAGR=15.3% MDD=-19.3% Cal=0.79 (408 trades)
#
# This file runs all 3 profiles in one sweep (3 configs)

static:
  strategy_type: momentum_top_gainers
  start_margin: 10000000
  start_epoch: 1262304000       # 2010-01-01
  end_epoch: 1773878400         # 2026-03-17
  prefetch_days: 600
  data_granularity: day

scanner:
  instruments:
    - [{exchange: NSE, symbols: []}]
  price_threshold: [50]
  avg_day_transaction_threshold:
    - {period: 125, threshold: 70000000}
  n_day_gain_threshold:
    - {n: 360, threshold: -999}

entry:
  momentum_lookback_days: [189]
  top_n_pct: [0.25]
  rebalance_interval_days: [42]
  min_momentum_pct: [5]
  direction_score_n_day_ma: [3]
  direction_score_threshold: [0.45, 0.50]
  regime_instrument: ['NSE:NIFTYBEES']
  regime_sma_period: [200]

exit:
  trailing_stop_pct: [22, 35]
  max_hold_days: [378]

simulation:
  default_sorting_type: [top_gainer]
  order_sorting_type: [top_gainer]
  order_ranking_window_days: [30]
  max_positions: [12, 30]
  max_positions_per_instrument: [1]
  order_value_multiplier: [1.0]
  max_order_value:
    - {type: fixed, value: 1000000000}

# Total: 2 entry x 2 exit x 2 sim = 8 configs (covers all 3 profiles + neighbors)
