# R1: broad sensitivity — all params together, modest grid
# 3 × 3 × 3 × 3 × 3 × 2 = 486 configs. Maybe too many. Reduce.
# 3 × 3 × 3 × 3 × 2 = 162 configs

static:
  strategy_type: momentum_dip
  start_margin: 10000000
  start_epoch: 1262304000
  end_epoch: 1773878400
  prefetch_days: 400
  data_granularity: day
  data_provider: nse_charting

scanner:
  instruments:
    - [{exchange: NSE, symbols: []}]
  price_threshold: [50]
  avg_day_transaction_threshold:
    - {period: 125, threshold: 70000000}
  n_day_gain_threshold:
    - {n: 360, threshold: -999}

entry:
  rsi_threshold: [25, 30, 35]
  momentum_lookback_days: [63, 126, 252]
  top_n: [50, 100, 150]
  rerank_interval_days: [21]

exit:
  profit_target_pct: [0.03, 0.05, 0.08]
  max_hold_days: [10, 21]

simulation:
  default_sorting_type: [top_gainer]
  order_sorting_type: [top_gainer]
  order_ranking_window_days: [30]
  max_positions: [15]
  max_positions_per_instrument: [1]
  order_value_multiplier: [1.0]
  max_order_value:
    - {type: fixed, value: 1000000000}
